# Changelog

本项目从 `v1.3.0` 开始统一 Chrome 扩展、Tampermonkey 用户脚本和 GitHub Release 的版本号。

## v1.3.2 — 2026-09-15

### 修复

- 修复胶囊按钮的 `inline-flex` 样式覆盖 HTML `hidden` 状态，导致“＋ 添加课程”错误出现在正常模式的问题。
- 为面板内所有带 `hidden` 属性的组件建立强制隐藏规则，避免今后的组件显示样式再次破坏模式可见性。
- 将模式、课程数量和运行状态对应的控件关系集中到统一状态函数：正常模式只显示第一门课程；多选模式才显示增减按钮；运行或查询期间统一锁定模式和增减操作。

### 验证

- 覆盖正常模式、多选模式、1/2/4 门课程及运行中状态的控件关系矩阵测试通过。
- JavaScript 语法、Chrome Manifest、版本一致性、配置迁移、轮询和连续确认弹窗测试通过。
- 正常模式与多选模式的 Chromium 离线渲染检查通过。

## v1.3.1 — 2026-09-15

### 改进

- 将多选模式中孤立的方形“＋”按钮改为待选课程标题栏右侧的“＋ 添加课程”胶囊按钮。
- 增加标题栏分隔、悬停反馈、键盘焦点样式、运行中禁用状态和四门课程上限提示。
- Chrome 扩展和 Tampermonkey 用户脚本保持相同布局与版本号。

### 验证

- JavaScript 语法、Chrome Manifest、版本一致性和多课程轮询测试通过。
- Google Chrome 离线面板渲染检查通过。

## v1.3.0 — 2026-09-15

### 新增

- 在配置面板顶部增加“正常模式”和“多选模式”选项卡。
- 多选模式支持通过加减按钮维护 1–4 个待选课程块。
- 每门课程可以独立设置匹配条件、最低余量和多班选择策略。
- 按课程块顺序循环查询，并在所有课程之间共享刷新间隔、随机抖动和最大查询次数。
- 多选模式的“仅轮询一轮”功能，以及查询过程中的停止和 `Esc` 支持。
- 旧版单课程配置自动迁移。
- 多课程数量限制、旧配置迁移和循环顺序自动测试。

### 保留

- 页面原生查询与选课流程。
- 连续班级确认、课程组确认及结果提示处理。
- 验证码、会话失效、错误退避、结果验证和安全暂停机制。

### 验证

- JavaScript 语法检查和 Chrome Manifest 解析通过。
- 双层确认弹窗冒烟测试通过。
- 四课程轮询与配置迁移测试通过。
- Google Chrome 离线面板渲染检查通过。

## 早期版本
- 本项目由私人论坛迁移至github，文件结构难免有所疏漏。
- Chrome 扩展曾使用 `v1.0.0` 和开发中的 `v1.1.0` 版本号。
- Tampermonkey 用户脚本曾使用 `v1.2.0`。
- GitHub 上的历史 `courses` Release 保留，不移动其标签。
